% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_classification.R
\name{classify_knn_cv_tuned}
\alias{classify_knn_cv_tuned}
\title{K-Nearest Neighbors Classification with Stratified k-Fold Cross-Validation}
\usage{
classify_knn_cv_tuned(
  features,
  labels,
  k_values = c(3, 5, 7, 9, 11),
  folds = 5
)
}
\arguments{
\item{features}{A matrix or data frame of numeric feature vectors (samples as rows, features as columns).}

\item{labels}{A factor or character vector representing the true class labels.}

\item{k_values}{A numeric vector representing the sequence of k values to test (default: c(3, 5, 7, 9, 11)).}

\item{folds}{Integer. The number of cross-validation folds (default: 5).}
}
\value{
A comprehensive list containing the summary \code{results}, the \code{best_k}, the \code{best_accuracy}, and the granular \code{fold_results}.
}
\description{
Implements a robust k-Nearest Neighbors (kNN) algorithm enveloped in stratified k-fold cross-validation to tune the optimal k hyperparameter without data leakage.
}
\details{
This function evaluates the classification accuracy of multiple k values. To ensure unbiased estimation, it partitions the data into \code{folds} using stratified sampling, guaranteeing that each fold maintains the overall class distribution.

\strong{Algorithm Steps:}
\enumerate{
\item Iterates through each candidate k in \code{k_values}.
\item For each fold, trains the model on k-1 folds and tests on the hold-out fold.
\item Averages the accuracy across all folds to determine the generalized performance of that specific k.
\item Identifies and recommends the optimal k value.
}
}
\examples{

data(bird_train)
features <- bird_train[, 1:15]
labels <- bird_train$species

# Run 5-fold CV to find the best k between 3 and 9
cv_results <- classify_knn_cv_tuned(features, labels, k_values = c(3, 5, 7, 9))
print(cv_results$best_k)

}
