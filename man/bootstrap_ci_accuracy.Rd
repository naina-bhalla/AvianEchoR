% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_classification.R
\name{bootstrap_ci_accuracy}
\alias{bootstrap_ci_accuracy}
\title{Bootstrap Confidence Intervals for Classification Accuracy}
\usage{
bootstrap_ci_accuracy(predictions, actual, n_bootstrap = 1000)
}
\arguments{
\item{predictions}{A vector of model-predicted labels.}

\item{actual}{A vector of the true ground-truth labels.}

\item{n_bootstrap}{Integer. The number of bootstrap iterations to perform (default: 1000).}
}
\value{
A list containing the \code{accuracy} point estimate, \code{ci_lower}, \code{ci_upper}, and the raw \code{bootstrap_samples}.
}
\description{
Computes the 95\% confidence interval for a model's accuracy score using non-parametric bootstrap resampling.
}
\details{
A single accuracy score on a test set is merely a point estimate. This function simulates standard error by resampling the paired predictions and actual labels with replacement \code{n_bootstrap} times. It calculates the accuracy of each resampled set and extracts the 2.5th and 97.5th percentiles to form a robust 95\% confidence interval.
}
\examples{

actual <- as.factor(c("Owl", "Owl", "Dove", "Dove", "Crow", "Crow"))
preds <- as.factor(c("Owl", "Dove", "Dove", "Dove", "Crow", "Crow"))

# Get 95\% CI based on 500 resamples
ci_data <- bootstrap_ci_accuracy(preds, actual, n_bootstrap = 500)
print(ci_data$ci_lower)

}
