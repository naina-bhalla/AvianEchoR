% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_classification.R
\name{create_stratified_folds}
\alias{create_stratified_folds}
\title{Create Stratified Folds for Cross-Validation}
\usage{
create_stratified_folds(labels, folds)
}
\arguments{
\item{labels}{A factor or character vector of class labels.}

\item{folds}{Integer. The total number of folds to partition the data into.}
}
\value{
A numeric vector containing the fold assignment (1 to \code{folds}) for each respective sample.
}
\description{
An internal utility that generates balanced fold assignments for cross-validation, ensuring proportional class representation in every fold.
}
\details{
Standard random sampling can accidentally create folds where rare bird species are entirely missing from the training or testing set. This function loops through each unique class and distributes its samples evenly across the specified number of folds.
}
\examples{
# Create an imbalanced dummy dataset of bird labels
bird_labels <- c(rep("Owl", 10), rep("Dove", 5), rep("Macaw", 5))

# Assign each sample to one of 5 stratified folds
fold_assignments <- create_stratified_folds(bird_labels, folds = 5)

# Verify that each fold gets exactly 2 Owls, 1 Dove, and 1 Macaw
table(Species = bird_labels, Fold = fold_assignments)
}
\keyword{internal}
