% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_load.R
\name{standardize_audio}
\alias{standardize_audio}
\title{Standardize Audio Properties for Feature Extraction}
\usage{
standardize_audio(wave, target_rate = 22050)
}
\arguments{
\item{wave}{A \code{Wave} object generated by the \code{tuneR} package.}

\item{target_rate}{Integer. The target sample rate in Hz (default: 22050).}
}
\value{
A standardized \code{Wave} object ready for Digital Signal Processing.
}
\description{
Normalizes audio structures by verifying object integrity, forcing stereo channels into mono, standardizing the sample rate, and normalizing bit-depth.
}
\details{
Mathematical feature extraction requires strictly uniform input data to prevent matrix dimension errors and scale skews. This function performs a 4-step standardization:
\enumerate{
\item Validates that the input is a non-empty \code{Wave} object.
\item Forcefully converts stereo recordings into mono by isolating the left channel.
\item Downsamples audio to the \code{target_rate} to ensure dimensional consistency. Warns if the audio is already below the target rate.
\item Normalizes the amplitude to a standard 16-bit scale so volume differences don't skew the machine learning model.
}
}
\examples{
\dontrun{
raw_wave <- read_audio_file("data-raw/audio/loud_bird.wav")

# Standardize to Mono, 22050 Hz, and 16-bit amplitude
clean_wave <- standardize_audio(raw_wave)

# Verify properties
print(clean_wave@stereo) # FALSE
print(clean_wave@samp.rate) # 22050
print(clean_wave@bit) # 16
}
}
